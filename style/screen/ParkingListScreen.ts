import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  flatListContainer: {
    width: "100%",
  },
  header: {
    width: "100%",
    paddingLeft: 10,
    paddingTop: 10,
  },
});
